{
  "name": "TaxWithholding",
  "type": "object",
  "properties": {
    "company_id": {
      "type": "string"
    },
    "employee_id": {
      "type": "string"
    },
    "jurisdiction": {
      "type": "string",
      "enum": ["federal", "state", "local"],
      "description": "An employee normally carries one active row per jurisdiction level simultaneously (e.g. a federal row and a state row) — this is not a single choice, it's a layer."
    },
    "filing_status": {
      "type": "string",
      "description": "e.g. 'single', 'married_filing_jointly', 'head_of_household' — free text since the valid set differs by jurisdiction/form (federal W-4 vs. a state's own withholding form)."
    },
    "allowances_or_credits": {
      "type": "number",
      "default": 0
    },
    "additional_withholding": {
      "type": "number",
      "default": 0,
      "description": "Extra flat amount withheld per pay period on top of the calculated withholding, in dollars."
    },
    "flat_rate_percent": {
      "type": "number",
      "default": 0,
      "description": "Simplified flat withholding rate for this jurisdiction/employee, used by calculateTaxesAndDeductions() in payrollEngine.js. This is NOT a substitute for real IRS Circular E / state percentage-method tables — building those bracket tables is out of scope here — but it keeps the rate admin-configurable master data instead of a number hardcoded into the engine, consistent with how PayrollRule.config works."
    },
    "effective_date": {
      "type": "string",
      "format": "date"
    }
  },
  "required": [
    "employee_id",
    "jurisdiction",
    "effective_date"
  ]
}
