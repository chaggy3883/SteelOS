#env
.env
.env.*
!.env.example

# Logs
/logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
.vite
schema/.app.jsonc

# Local flash-drive-portable data mirror (src/api/localData.js /
# vite.config.js /__db-sync) — this is a live local DB snapshot, not source;
# it travels with the project folder itself (e.g. on a USB drive), not git.
db.json

# browser-testing skill: local Edge profiles (hold logged-in localStorage
# per demo role) and captured screenshots — machine-local test artifacts.
.claude/skills/browser-testing/.edge-profiles
.claude/skills/browser-testing/.screenshots
